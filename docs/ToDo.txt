logs:
    messages reçus par un acteur donné + sender
    messages envoyés par un acteur donné + recipient

refactor Message en dict?
refactor festion path -> pathlib?
merge fix_encoding et display.constants.character_encoding
    belle api fonctionelle?


Implémentation:
    loop mode
        intégration playlist
        gui
        event refresh gui


api:
    async messaging
    request response
    pub sub


bug:
    /Audio/Music/Electro
    height=25 width=58 y_ofst=0 x_ofst=0
    row=24 col=29 max_col=29 display_mode=2 height=25 width=58 err=error('addwstr() returned ERR')
    On ne peut pas écrire dans la dernière row d'une fenetre? mais on peut faire une box
